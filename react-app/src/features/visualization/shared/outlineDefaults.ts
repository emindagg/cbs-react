/**
 * İl/İlçe dış sınır (outline) için paylaşılan varsayılanlar.
 * Choropleth, bubble backdrop ve dot backdrop aynı değerleri kullanır.
 */
export const DEFAULT_OUTLINE_COLOR = '#010109'
export const DEFAULT_OUTLINE_OPACITY = 1
